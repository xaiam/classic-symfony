<html>
<body>
<div>
    <h1>公園情報</h1>
    <ul>
        {% for concertInfo in concertList %}
            <li>{{ loop.index }}
                {{ concertInfo.date }}
                {{ concertInfo.place }}
                {% if concertInfo.available is same as(true) %}
                    (予約可能)
                {% else %}
                    (予約不可)
                {% endif %}
                {{ concertInfo.time }}開演
            </li>
        {% endfor %}
    </ul>
</div>
</body>
</html>